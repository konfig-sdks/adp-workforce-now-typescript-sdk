import { AdpWorkforceNow } from "./index";

describe("adp-workforce-now-typescript-sdk", () => {
    it("initialize client", async () => {
        const adpworkforcenow = new AdpWorkforceNow({
        });
    });
});
